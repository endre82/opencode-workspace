# Shared OpenCode Configuration

This directory contains shared OpenCode configuration files.
Files here will be mounted read-write to all environments.

Place global model definitions and shared provider configurations here.
